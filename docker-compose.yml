version: '3'
services:
  postgres:
    image: postgres
    restart: always
    volumes:
      - ./postgres/:/docker-entrypoint-initdb.d
    environment:
      - POSTGRES_PASSWORD=password
    ports:
      - "5432:5432"
  managym:
    build: ./app/
    restart: always
    ports:
      - "4567:4567"
    depends_on:
      - postgres